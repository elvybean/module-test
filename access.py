# OUTER ###############################################################
# outside a seperate directory and in the same directory as the package
#######################################################################
import num_lib

num_lib.One()
num_lib.Two()
num_lib.Three()
